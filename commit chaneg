git commit --amend -m "Updated message for the previous commit"

git rebase -i HEAD~4
# Replace '4' with the number of commits you need to modify historically.
# In the editor, change 'pick' to 'reword' for the desired commits.